---
alwaysApply: true
---

This project represents a single site instances inside a larger multisite Drupal installation hosted on Acquia.

The docroot folder is located several directories above the git repository root.

When running `drush` commands, always use `ddev drush @carlsonschool.ddev`.
