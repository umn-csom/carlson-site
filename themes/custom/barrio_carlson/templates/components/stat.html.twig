<div class="col-md">
	<div class="stats__single text-center">
	  <label class="stats__single-value">{{ statistic_value | default('96%') }}</label>
	  <p class="stats__single-desc">{{ statistic_desc | default('96% of graduates received job offers 90 days after graduation.') }}</p>
	</div>
</div>