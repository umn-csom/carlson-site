{% block paragraph %}
    {% block content %}
        {{ attach_library('barrio_carlson/iframe-library') }}
        <div class="iframe-container">
            <iframe title="{{ content.field_iframe_title }}" src="{{ content.field_link }}" frameborder="0" scrolling="no"></iframe>
        </div>
    {% endblock %}
{% endblock paragraph %}