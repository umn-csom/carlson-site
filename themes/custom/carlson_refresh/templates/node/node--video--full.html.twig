
{#
/**
 * @file
 * Bootstrap Barrio's theme implementation to display a node.
 */
#}

<article{{ attributes.addClass(classes) }}>
  <header>
    {{ title_prefix }}
    {% if page %}
      <h1 class="node__title">{{ label }}</h1>
    {% endif %}
    {{ title_suffix }}
  </header>
  <div{{ content_attributes.addClass('node__content', 'clearfix') }}>
   <div class="row">
      <div class="col-md-8 offset-md-2">
        {{ content }}
      </div>
    </div>
  </div>
</article>

{# <div class="row video-items__item">
  {% if content.field_video_thumb|field_value is not empty %}
    <div class="col-3 video-items__item--image">
      {{ content.field_video_thumb|field_value }}
    </div>
    <div class="col-9 video-items__item--text">
      <h2 class="video-items__item--title h4">{{ label }}</h2>
      {{ attach_library('carlson_refresh/ui-dialog') }}
      <a class="use-ajax video-items__item--link" data-dialog-type="modal" href="/media/{{ content.field_video.0 }}" data-dialog-options="{&quot;width&quot;:800}">Watch Video</a>
    </div>
  {% else %}
    <div class="col-12">
      <h2 class="video-items__item--title h4">{{ label }}</h2>
      <a href="{{ url }}" class="video-items__item--link">Watch Video</a>
    </div>
  {% endif %}
</div> #}
