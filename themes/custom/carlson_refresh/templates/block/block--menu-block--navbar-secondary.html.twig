
<div{{ attributes.removeClass('contextual-region').addClass('d-flex', 'flex-row') }}>
  {% if label %}
  <strong class="site-nav__sl">
    {# @todo ensure is-active class gets added to the linked label #}
    {{ label }}
  </strong>
  {% endif %}
  {{ content }}
</div>
