{#
/**
 * @file
 * Theme override for the executive education catalog link field.
 */
#}
{# {{ attach_library('carlson_refresh/catalog-link') }} #}
{% set url = items.0.content['#url'] %}
{% set title = items.0.content['#title'] %}
{% if url is not empty and title is not empty %}
<a href="{{ url }}" class="btn btn-link">
  {{ title }}&nbsp;&nbsp;{{ source(directory ~ '/images/svg/arrow-right-cta.svg')}}
</a>
{% endif %}

