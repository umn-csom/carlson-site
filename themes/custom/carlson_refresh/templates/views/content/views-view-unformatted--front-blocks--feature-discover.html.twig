{#
/**
 * @file
 * Theme override to display front_blocks feature_discover rows.
 */
#}
{% for row in rows %}
  <div{{ row.attributes.addClass(default_row_class ? 'views-row') }}>
    {{ row.content }}
  </div>
{% endfor %}
